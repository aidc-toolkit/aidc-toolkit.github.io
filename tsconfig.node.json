{
  "extends": "@aidc-toolkit/dev/tsconfig.json",
  "include": ["src/**/*", "eslint.config.ts", ".vitepress/config.mts"],
  "compilerOptions": {
    // Emit.
    "noEmit": true,

    // Interop constraints.
    "isolatedModules": true,

    // Language and environment.
    "moduleDetection": "force"
  }
}
